---
const currentYear = new Date().getFullYear();

const footerSections = {
  boutique: {
    title: "Boutique",
    links: [
      { href: "/configurateur", label: "Configurateur" },
      { href: "/collections", label: "Collections" },
      { href: "/nouveautes", label: "Nouveautés" },
      { href: "/promos", label: "Promotions" }
    ]
  },
  service: {
    title: "Service Client",
    links: [
      { href: "/contact", label: "Nous contacter" },
      { href: "/faq", label: "FAQ" },
      { href: "/livraison", label: "Livraison & Retours" },
      { href: "/garantie", label: "Garantie" }
    ]
  },
  apropos: {
    title: "À Propos",
    links: [
      { href: "/histoire", label: "Notre Histoire" },
      { href: "/artisans", label: "Nos Artisans" },
      { href: "/engagement", label: "Nos Engagements" },
      { href: "/presse", label: "Presse" }
    ]
  }
};

const socialLinks = [
  { href: "#", label: "Instagram", icon: "instagram" },
  { href: "#", label: "Facebook", icon: "facebook" },
  { href: "#", label: "Twitter", icon: "twitter" },
  { href: "#", label: "LinkedIn", icon: "linkedin" }
];
---

<footer class="bg-primary text-neutral-content mt-20">
  <!-- Contenu principal -->
  <div class="max-w-7xl mx-auto px-6 py-16">
    <div class="grid lg:grid-cols-4 gap-12">
      
      <!-- Colonne marque -->
      <div class="lg:col-span-1">
        <div class="flex items-center gap-2 text-2xl font-serif font-semibold mb-4">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7z"/>
            <circle cx="12" cy="12" r="3"/>
          </svg>
          TaVue
        </div>
        <p class="text-sm opacity-70 leading-relaxed mb-6">
          L'excellence de la lunetterie française. Créez des montures élégantes, raffinées et parfaitement adaptées à votre style.
        </p>
        
        <!-- Réseaux sociaux -->
        <div class="flex gap-3">
          {socialLinks.map(social => (
            <a 
              href={social.href}
              aria-label={social.label}
              class="btn btn-circle btn-sm btn-ghost opacity-70 hover:opacity-100"
            >
              <span class="text-xs font-semibold">{social.icon.slice(0, 2).toUpperCase()}</span>
            </a>
          ))}
        </div>
      </div>

      <!-- Colonnes de liens -->
      {Object.values(footerSections).map(section => (
        <div>
          <h4 class="font-semibold text-base mb-4">{section.title}</h4>
          <ul class="space-y-3">
            {section.links.map(link => (
              <li>
                <a 
                  href={link.href}
                  class="text-sm opacity-70 hover:opacity-100 transition-opacity inline-block"
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>

  <!-- Barre inférieure -->
  <div class="border-t border-base-100/10">
    <div class="max-w-7xl mx-auto px-6 py-6">
      <div class="flex flex-col md:flex-row items-center justify-between gap-4 text-sm opacity-60">
        <div class="flex flex-wrap items-center gap-4">
          <span>© {currentYear} TaVue. Tous droits réservés.</span>
          <span class="hidden md:inline">•</span>
          <span class="flex items-center gap-1.5">
            Fabriqué en France 
            <span class="text-base">🇫🇷</span>
          </span>
        </div>
        
        <div class="flex flex-wrap items-center gap-4">
          <a href="/mentions-legales" class="hover:opacity-100 transition-opacity">Mentions légales</a>
          <span>•</span>
          <a href="/confidentialite" class="hover:opacity-100 transition-opacity">Confidentialité</a>
          <span>•</span>
          <a href="/cgv" class="hover:opacity-100 transition-opacity">CGV</a>
        </div>
      </div>
    </div>
  </div>
</footer>